/**
 * Icon adapter (server components).
 *
 * The ONLY place allowed to import the icon vendor's SSR entry point.
 * Client components import from "./index" instead.
 */
export * from "@phosphor-icons/react/dist/ssr";
